# Settings apply to all WSL 2 distros
[wsl2]

# 限制 WSL 2 虚拟机可使用的最大内存
# 对于编译大型项目，建议至少 8GB，16GB 更佳
memory=100GB

# 限制分配给 WSL 2 的 CPU 核心数
# 建议设为您物理机逻辑处理器数量的一半或更多
# 您可以在任务管理器 -> 性能 -> CPU 中看到逻辑处理器数量
processors=20

# 设置交换空间大小。
# 如果内存充足，可以设为 0 以避免因使用慢速的磁盘交换而导致性能下降。
# 如果内存可能不足，可以设置一个值（如8GB）以防止编译因内存溢出而崩溃。
swap=10GB

# [可选] 改善某些网络场景下的连接稳定性
localhostForwarding=true